# @title Birds
# @description this function provides the average number of birds across all the safari's
# @param x is the input values of the data you find
# @ return resulting constant for main function

birds <- function(x){
  my_birds <- (mean(x))
  return(my_birds)
}
